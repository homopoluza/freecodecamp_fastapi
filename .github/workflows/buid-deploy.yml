name: Build and Deploy Code

on:
  push:
    branches:
    - "main"
    - "master"
  pull_request:
    branches:
    - "main"
    - "master"

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v7